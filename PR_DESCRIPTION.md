# Planner-Actor パターンエンジンと堅牢なツール呼び出し抽出の導入

このプルリクエストでは、高推論モデル（Qwen 3.5、DeepSeek-R1 など）をコーディングエージェントとして効果的に機能させるため、実際のツール実行を専門モデルに委託する「Planner-Actor パターン」の基盤エンジンを導入します。本 PR は「仕組み（メカニズム）」の構築に焦点を当てており、将来のモデル固有のアップデートがスムーズに動作するための堅牢な土台を提供します。

## 主な変更点 (初期投稿時)

### 1. Planner-Actor オフローディング (TOOLUSE_MODEL)
- **構造的オフローディング・トリガー**: 推論モデル（Planner）が実行意図を示したものの、構造化されたツール呼び出しを出力できなかった場合に、専門モデル（Actor）へ実行を自動的に委託する仕組みを導入しました。
- **判定ロジックの改善**: XMLタグ、JSONマーカー、または思考ブロック（`</think>`）の完了状態に基づく構造的な検知に切り替え、精度を向上させました。
- **Actor 専用コンテキスト**: Actor モデル向けに、特化型のシステムプロンプト（英語）を実装しました。

### 2. 堅牢なツール呼び出し抽出
- **Balanced Braces（括弧の対応関係）のサポート**: 多行にわたる JSON や深くネストされたオブジェクトを確実に抽出。
- **思考ブロック内からの抽出**: `<think>` タグ内に含まれるツール呼び出しもスキャン対象化。

---

# 2026-03-29 更新：指摘事項の完全解消と Qwen 3.5 への基盤対応

再度動作確認を行い、これまでの議論や指摘を完全に反映させる形で内容を更新しました。

## 今回のアップデート内容

### 1. 堅牢なツール呼び出し抽出の完成 (Issue #17)
- **コードブロック内スキャンの解禁**: 推論モデルが ` ```json ` 等のブロック内にツール呼び出しを記述するケースに対応し、全テキストからの正確な抽出をサポートしました。
- **抽出ロジックの統合**: 標準 XML、Qwen 形式 XML、生の JSON のすべてを単一パスで効率的かつ漏れなく処理できるようリファクタリングを完了しました。

### 2. 推論型モデルへの基盤対応（Qwen 3.5 等）
- **検知ロジックの強化**: 単語境界を考慮した正規表現により、`qwen3.5`、`deepseek-r1`、`o1` 等を正確に識別します。
- **コンテキスト管理**: `MODEL_CONTEXT_SIZES` に Qwen 3.5 系の定義を追加し、大規模コンテキストを手動利用時でも適切に扱えるようにしました。
- **[重要] PR #20 との役割分担**: Qwen 3.5 を標準の推奨モデルとして**自動検出（MODEL_TIERS）**に組み込む対応は、後続の PR #20 にて行います。本 PR は、推論モデルが確実に機能するための「エンジン」を提供することに専念しています。

### 3. 安定性の向上
- **ループ停止条件の修正**: ユーザーによる `abort` (Ctrl+C 等) や、3 回連続のツール実行拒否時に、エージェントループが即座かつ確実に停止するよう修正しました。
- **パス正規化の最適化**: `_norm_args` 内での `os.getcwd()` 呼び出しを最適化し、ループ検知のパフォーマンスを向上させました。

## 検証結果
- **自動テスト**: `tests/test_vibe_coder.py` の全 857 テストをパス。
- **実機検証**: DeepSeek-R1 および Qwen 3.5 において、コードブロック内からの JSON 抽出および Actor へのオフロードが正常に機能することを確認済み。
