# Planner-Actor パターンエンジンと堅牢なツール呼び出し抽出の導入

このプルリクエストでは、高推論モデル（Qwen 3.5、DeepSeek-R1 など）をコーディングエージェントとして効果的に機能させるため、実際のツール実行を専門モデルに委託する「Planner-Actor パターン」の基盤エンジンを導入します。本 PR は「仕組み（メカニズム）」の構築に焦点を当てており、将来のモデル固有のアップデート（PR #20 の Qwen 3.5 対応など）がスムーズに動作するための堅牢な土台を提供します。

## 主な変更点

### 1. Planner-Actor オフローディング (TOOLUSE_MODEL)
- **構造的オフローディング・トリガー**: 推論モデル（Planner）が実行意図を示したものの、構造化されたツール呼び出しを出力できなかった場合に、専門モデル（Actor）へ実行を自動的に委託する仕組みを導入しました。
- **判定ロジックの改善**: 従来の脆弱なキーワードベースの判定を廃止し、XMLタグ、JSONマーカー、または思考ブロック（`</think>`）の完了状態に基づく構造的な検知に切り替え、精度を大幅に向上させました。
- **Actor 専用コンテキスト**: Actor モデル向けに、プラットフォーム情報やカレントディレクトリ等の環境情報を含む特化型のシステムプロンプト（英語）を実装し、履歴の重複を排除してツール実行の精度を高めました。

### 2. 堅牢なツール呼び出し抽出
- **Balanced Braces（括弧の対応関係）のサポート**: `_extract_tool_calls_from_text` を強化し、括弧の対応関係を解析することで、多行にわたる JSON や深くネストされたオブジェクトを確実に抽出できるようにしました。
- **思考ブロック内からの抽出**: 思考ログ（`<think>` タグ内）に含まれるツール呼び出しもスキャン対象とし、推論プロセスの中に現れた実行意図を漏らさず検知します。
- **生の JSON 抽出の強化**: XML ラッパーがない OpenAI 互換の生の JSON 形式のツール呼び出しにも対応しました。

### 3. 推論型モデルへの最適化
- **`_is_reasoning_model` の検知強化**: 単語境界や構造パターンを用いた正規表現により、`qwen3.5`、`r1`、`o1` などのモデルを正確に識別し、誤検知を防ぎます。
- **Planner プロンプト（日本語）の維持**: 本体への影響を最小限にするため、Planner 側のシステムプロンプトは従来の日本語の指示を維持しつつ、ツール利用の効率を高めています。

## 技術的詳細
- **抽出ロジックの統合**: 標準的な XML、Qwen 形式の XML、および生の JSON といった多様なモデル出力フォーマットを、単一のパスで効率的に処理できるようにリファクタリングしました。
- **ロールの分離**: 「推論（Planner）」と「実行（Actor）」の役割を明確に分離することで、将来的に様々なモデルを自由に組み合わせて利用できるインフラを整えました。

## 検証結果
- **自動テスト**: `test_vibe_coder.py` に含まれる全 857 個のテストをすべて通過しました。
- **ロジック検証**: 専用の検証スクリプトを用い、括弧の対応関係に基づく抽出ロジックとオフローディング・トリガーが意図通り動作することを実証済みです。
- **手動確認**: Planner が日本語の指示に従い、Actor が専用の英語指示でツールを実行することを確認しました。
