# [更新] Planner-Actor パターン基盤と堅牢なツール呼び出し抽出 (Issue #17)

このプルリクエストでは、DeepSeek-R1 や Qwen 3.5 といった高推論モデルを実用化するための核心的な仕組みである「Planner-Actor パターン」と、Issue #17 で指摘されていた「ツール呼び出し抽出の堅牢化」を導入します。

当初の設計からさらに踏み込み、コードブロック内からの抽出やネストされた JSON への対応を完了させることで、推論モデルが自由な形式で思考・出力した場合でも確実にツールを実行できる環境を整えました。

## 主な変更点

### 1. 堅牢なツール呼び出し抽出 (Issue #17 の完全解消)
従来の抽出ロジックにおける脆弱性を排除し、あらゆる出力パターンからツール呼び出しを正確に復元します。
- **Balanced Braces 対応**: 括弧の対応関係を解析するアルゴリズムを導入。多行にわたる JSON や深くネストされたオブジェクトも、構造を壊さず抽出可能になりました。
- **コードブロック内スキャンの解禁**: 推論モデルが ` ```json ` 等のブロック内にツール呼び出しを記述するケースに対応し、全テキストからの抽出をサポートしました。
- **思考ブロック内からの検知**: `<think>` タグ内に現れたツール実行意図も漏らさずスキャン対象とします。

### 2. Planner-Actor オフローディング (TOOLUSE_MODEL)
推論モデル（Planner）が「実行の意思」を示しながらも構造化された出力に失敗した場合、 Actor モデル（`TOOLUSE_MODEL`）へ自動的に処理を委託する基盤を導入しました。
- **Actor 専用プロンプト**: プラットフォームやカレントディレクトリ情報を内置した Actor 特化型の指示（英語）により、実行の正確性を最大化。
- **シームレスな連携**: プランナーの思考プロセスを Actor に適切に引き継ぐことで、複雑な指示でも実行まで完遂させます。

### 3. 推論型モデルへの基盤対応（Qwen 3.5 等）
PR #20（自動推奨設定）に先立ち、本 PR の範疇である「仕組み」の部分において、Qwen 3.5 を含む推論モデルへの対応を完了させました。
- **検知ロジックの強化**: 単語境界を考慮した正規表現により、`qwen3.5`、`deepseek-r1`、`o1` 等を正確に識別します。
- **コンテキスト管理**: `MODEL_CONTEXT_SIZES` に Qwen 3.5 系の定義を追加しました。
- **[補足] PR #20 との役割分担**: Qwen 3.5 を標準の推奨モデルとして**自動検出（MODEL_TIERS）**に組み込み、推奨スペック表を更新する対応は、後続の PR #20 にて行います。本 PR は、推論モデルを手動指定した際にそれらが確実に機能するための「エンジン」を提供することに専念しています。

## 安定性とパフォーマンスの改善
- **エージェントループの制御**: ユーザーによる `abort` や、3 回連続のツール実行拒否時に、ループを即座に停止するよう修正しました。
- **パス正規化の最適化**: `_norm_args` 内での `os.getcwd()` 呼び出しをキャッシュ化し、ループ検知のオーバーヘッドを削減しました。

## 検証結果
- **自動テスト**: `tests/test_vibe_coder.py` の全 857 テストをパス。
- **実機検証**: DeepSeek-R1 および Qwen 3.5 を用い、コードブロック内からの JSON 抽出および Actor へのオフロードが正常に機能することを確認済み。
